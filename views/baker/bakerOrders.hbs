<main>
    {{#each orders}}
    <div class="order-cards card">
        <div class="card-content">
            <div class="media">
                <div class="media-left">
                    <figure class="image is-96x96">
                        <img class="order-image" src="{{cakeId.image}}" alt="">
                    </figure>
                </div>
                <div class="orders media-content media-right">
                    <h3>Name: {{cakeId.name}}</h3>
                    <h3>Quantity: {{quantity}}</h3>
                    <form action="/baker/orders/{{_id}}?_method=PUT" method="POST">
                        <label class="order-status" for="status">Status</label>
                        <input class="input is-medium is-rounded is-warning" type="text" name="status"
                            value="{{status}}">
                        <div class="submit">
                            <input class="button" type="submit" name="Update Status">
                        </div>
                    </form>
                    <form action="/baker/orders/{{_id}}?_method=DELETE" method="POST">
                        <div class="submit">
                        <input class="button" type="submit" value="Delete Order">
                        </div>
                    </form>
                    </h3>
                </div>
            </div>
        </div>
    </div>
    {{/each}}
</main>